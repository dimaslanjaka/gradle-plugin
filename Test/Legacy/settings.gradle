rootProject.name = 'Test Legacy Method'


// Declare plugin relative path for direct test
File plugin = new File(rootProject.projectDir, "/../../")

// For direct change listen
includeBuild(plugin.getAbsolutePath())

include(":java-groovy-kotlin")